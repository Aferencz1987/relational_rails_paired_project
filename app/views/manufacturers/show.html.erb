<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width-device-width, initial-scale=1">
</head>
<%= link_to "Update #{@manufacturer.brand}", "/manufacturers/#{@manufacturer.id}/edit" %>
<h2><%= @manufacturer[:brand] %></h2>
<p>
  This manufacturer is based
  <% if @manufacturer[:domestic] == true then %>
    in the US.
  <% else %>
    overseas.
  <% end %>
  <%= @manufacturer[:brand] %>'s facility hasn't had a serious safety incident in <%= @manufacturer[:days_since_last_incident] %> days.
</p>
<h4>Buy now! <%= @manufacturer.brand %> has <%= @num_of_guitars %> guitars.</h4>
<a href="<%= url_for(action: 'inventory', controller: 'manufacturers') %>">Guitar Inventory</a>
